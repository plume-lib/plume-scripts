version: 2.1
jobs:
  build:

    docker:
      - image: ubuntu

    environment:
      TERM: dumb

    steps:

      - restore_cache:
          keys:
            - source-v1-{{ .Branch }}-{{ .Revision }}
            - source-v1-{{ .Branch }}-
            - source-v1-
      - checkout
      - save_cache:
          key: source-v1-{{ .Branch }}-{{ .Revision }}
          paths:
            - ".git"

      - run: cat /etc/os-release
      - run: apt update
      - run: apt install -qy curl
      - run:
          name: Install uv
          command: |
            curl -LsSf https://astral.sh/uv/install.sh | sh
            which uv
            uv --version
      - run:
          name: Install docker
          command: |
            curl -fsSL https://get.docker.com -o get-docker.sh
            sh get-docker.sh
            which docker
            docker --version
            docker version
      - run:
          name: Install dependencies for "make style-check"
          command: apt install -qy make shellcheck shfmt wget yamllint

      - run: make showvars
      - run: make

workflows:
  build:
    jobs:
      - build
